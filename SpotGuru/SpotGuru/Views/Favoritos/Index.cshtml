@using Helpers
@model IEnumerable<SpotGuru.Models.Favoritos>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Favorites</h1>

<div>
    <link rel="stylesheet" href="/css/monumentDetails.css" />

    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayName("Name")
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Monumentos.Latitude)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Monumentos.Longitude)
                </th>
                <th>
                    @Html.DisplayName("Category")
                </th>
            </tr>
        </thead>
    
        <tbody>
            @foreach (var item in Model) {
                <tr class="mon_miniature_section">
                    <td>
                        <a asp-action="Details" asp-controller="Monumentos" asp-route-id="@item.Monumentos.Id">@Html.DisplayFor(modelItem => item.Monumentos.Nome)</a>
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Monumentos.Latitude)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Monumentos.Longitude)
                    </td>
                    <td>
                        @Html.DisplayName(EnumExtensionMethods.GetDescription(item.Monumentos.Categoria))
                    </td>
                    <td>
                        @{
                            string imagePath = "/images/monuments/" + item.Monumentos.pathFoto;
                        }
                    <img class="img_mon_miniature" src=@imagePath alt="Not Found" />
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
